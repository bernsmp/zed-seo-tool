name: Keep Alive

on:
  schedule:
    - cron: "*/25 * * * *" # every 25 minutes
  workflow_dispatch: # manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -sf --max-time 30 "https://zed-seo-tool.streamlit.app/" -o /dev/null
